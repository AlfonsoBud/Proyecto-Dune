using System;

namespace AdminClient
{
    // Minimal stub used by the AdminClient program to compile.
    public class ApiClient
    {
        public ApiClient(string apiUrl, string bearerToken, string userBearerToken)
        {
            // Intentionally empty stub for compile-time only.
        }
    }
}
